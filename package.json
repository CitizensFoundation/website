{
  "name": "citizens-is-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs && node scripts/generate-seo.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "cheerio": "^1.2.0",
    "sharp": "^0.35.1",
    "turndown": "^7.2.4",
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=20"
  }
}
